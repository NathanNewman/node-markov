{
  "name": "markov",
  "version": "1.0.0",
  "description": "It's a machine that generates random text.",
  "main": "makeText.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nate",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2"
  }
}
